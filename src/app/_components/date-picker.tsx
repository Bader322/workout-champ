"use client";
import { Datepicker } from "flowbite-react";

const datePicker = () => {
  return (
      <Datepicker
        autoHide={true}
        type="text"
        className=""
      />
  );
};

export default datePicker;
